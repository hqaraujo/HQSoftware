

function Header(props) {
    return (
        <div>
            <h1>Bem vindo {props.nome}, tudo bem?</h1>
        </div>
    )
}
export default Header

// Quando usar mais de um props é so chamar pelo nome de cada variavél e nao 
// escrever props, deixara o codigo mais organizado, tem que declarar dentro da função.

//
//
//